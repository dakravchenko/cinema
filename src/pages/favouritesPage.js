// import { data, favoriteTags } from "../data";

// function addFavorite(event){
//     if (event.target.classList.contains("video-like")){
//         const favoriteMovie = JSON.parse(event.target.getAttribute("data-info"));
//         event.target.parentElement.addEventListener("click", addFavorite);

//         if(!favoriteTags.includes(favoriteMovie.id)){
//             favoriteTags.push(favoriteElement.id);
//             data.push(favoriteMovie)

//         } else {
//             let elementNumber;
//             const favoriteLink = document.querySelectorAll(".menu-item")[1];
//             favoriteTags.forEach((element, index) => {
//                 if (element === favoriteElement.imdbID) {
//                     elementNumber = index;
//                 }
//         }
//         }
//     }